using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetLoadOnLanding : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        SetLibrary.loadSets();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
